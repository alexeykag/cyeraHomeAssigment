import { BaseComponent, IBaseComponent } from "../baseComponent";

/**
 * ProductPrice component
 */

export class ProductPrice extends BaseComponent {
    constructor(dataProps: IBaseComponent) {
        super(dataProps);
    }
}
